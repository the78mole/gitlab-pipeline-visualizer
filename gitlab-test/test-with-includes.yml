include:
  - local: 'gitlab-test/ci-build.yml'
  - local: 'gitlab-test/ci-test.yml'

stages:
  - build
  - test
  - deploy

variables:
  DOCKER_IMAGE: myapp:latest

deploy:staging:
  stage: deploy
  needs:
    - test:unit
    - test:integration
  script:
    - kubectl apply -f k8s/staging/
